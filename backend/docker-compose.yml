version: "3.9"

services:
  db:
    image: postgres:16
    container_name: product_review_postgres2
    restart: always
    environment:
      POSTGRES_DB: product_db
      POSTGRES_USER: product_user
      POSTGRES_PASSWORD: password
    ports:
      - "5435:5432"
    volumes:
      - product_review_postgres_data:/var/lib/postgresql/data

volumes:
  product_review_postgres_data: